# Decisions

Shared, agent-authored log of cross-cutting decisions the fleet must honor. The conductor records plan/rebase/steer decisions here; workers record interface contracts + adjustments. Each entry is a `##` heading with **What / Why / By** so it scans. Re-read before each significant step; jj merges concurrent edits as first-class conflicts on fan-in.

## worker: Fixed mwitch showing only one Ghostty window. Commit 272e47c
- **Did:** Fixed mwitch showing only one Ghostty window. Commit 272e47c collapsed Ghostty CG windows sharing a quantized visual frame to hide background tab surfaces, but two real Ghostty windows zoomed to the same size collapse into one — verified live on this machine (windows 78 rudder and 76 aws-v2, both at 0,33 1728x1084; only 76 was listed). Replaced the frame heuristic with a Space-membership check: the window server assigns every real window to a Space (including other Spaces and fullscreen Spaces), while native NSWindow tab surfaces belong to no Space at all. Added Sources/mwitch/WindowSpaces.swift wrapping the private CGSCopySpacesForWindows (taken as Unmanaged and released, since Copy returns +1), and applied the check only in the branch where AX cannot resolve an offscreen window — so minimized/AX-resolvable windows are structurally unaffected. A failed query returns nil and keeps the window. Fix is app-agnostic, so it also covers Terminal and other NSWindow-tabbed apps.
- **Interfaces:** NEW Sources/mwitch/WindowSpaces.swift: enum WindowSpaces with static isAssignedToASpace(CGWindowID) -> Bool? (nil = query failed). Global @_silgen_name decls CGSMainConnectionID() and CGSCopySpacesForWindows(_:_:_:) -> Unmanaged<CFArray>?, plus typealias CGSConnectionID = Int32. CHANGED WindowEnumerator.entries(...) gained a required trailing parameter isAssignedToASpace: (CGWindowID) -> Bool?; enumerate() passes WindowSpaces.isAssignedToASpace. REMOVED private WindowEnumerator.VisualFrameKey and the Ghostty frame-collapse block.
- **Follow-ups:**
  - Verify off-Space behavior on a multi-Space setup [out of lane] — This machine has exactly one Space, so the assumption that a window on another Space reports a non-empty Space list could not be exercised locally. It is the documented purpose of the 0x7 mask and what AltTab relies on, and the nil-keeps-the-window fallback limits the blast radius, but a two-Space smoke test would confirm it directly.
  - Confirm a minimized native-tab window still lists [out of lane] — No minimized window existed during testing. AX returns minimized windows so they take the axInfo != nil branch and never reach the Space check (covered by a unit test), but a live confirmation would close the loop.
- **By:** worker · 2026-07-30T18:04:03.189Z

## conductor: Hardened Space-membership semantics after live end-to-end audit
- **What:** Space-less offscreen windows are rejected only when they are neither minimized nor owned by a hidden app. Nonempty Space membership is treated as stronger identity evidence than title de-duplication, and private CGS functions are resolved dynamically so missing symbols fail open.
- **Why:** Live Ghostty data proves Space membership separates the two logical windows from tab surfaces, but an empty Space list is a broader invisible-window bucket that can include minimized and hidden windows. Same-title windows on other Spaces are also legitimate and must not be collapsed.
- **By:** conductor · 2026-07-31
